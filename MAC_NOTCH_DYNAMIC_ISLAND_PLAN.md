# Mac 刘海灵动岛开发文档（ohBangs）

## 1. 目标
在 macOS 上实现一个常驻顶部、围绕刘海区域的“灵动岛”交互层，支持：
- 常驻收起态（小胶囊）
- 展开态信息卡片（如音乐、计时器、消息）
- 动画过渡与交互响应
- 可扩展的数据源与插件化内容模块

## 2. 平台与技术栈
- 语言：Swift
- UI：SwiftUI（主视图）+ AppKit（窗口层级/多屏适配）
- 最低系统建议：macOS 13+
- 工程：当前 `ohBangs` 项目

## 3. 产品形态
### 3.1 收起态
- 位于主屏顶部中央，视觉上贴合刘海区域
- 默认展示图标/状态点（例如播放中、计时中）
- 鼠标悬停可高亮

### 3.2 展开态
- 点击或悬停触发展开
- 展示当前活动卡片（音乐、通知、快捷操作）
- 支持自动收起（超时或失焦）

### 3.3 异常与降级
- 无刘海机型：显示在顶部中间“仿刘海区域”
- 全屏 App 场景：根据权限和层级策略决定隐藏或悬浮

## 4. 架构设计
### 4.1 模块划分
- `IslandWindowManager`
  - 管理 NSWindow 创建、层级、位置、可见性
- `IslandStateStore`
  - 管理 UI 状态：收起/展开/动画中/当前卡片
- `IslandView`（SwiftUI）
  - 负责灵动岛外观、动画、手势
- `IslandContentProvider`
  - 内容源协议（音乐、计时器、系统状态）
- `IslandAnimationEngine`
  - 统一控制过渡曲线、弹性和中断恢复

### 4.2 数据流
1. Provider 推送状态变更到 `IslandStateStore`
2. Store 发布 UI 状态
3. `IslandView` 根据状态执行动画和内容切换
4. 用户交互事件回写 Store，触发窗口层行为

## 5. 关键实现点
### 5.1 顶部悬浮窗口
- 使用 `NSWindow` + 无标题栏 + 透明背景
- 窗口层级：高于普通窗口（避免被遮挡）
- 禁止抢焦点（避免打断当前操作）

### 5.2 刘海区域定位
- 通过 `NSScreen` 获取主屏尺寸和安全区域（可结合经验偏移）
- 以“顶部中心锚点”计算胶囊 frame
- 多屏时仅主屏显示，或支持配置显示屏

### 5.3 动画系统
- 收起 <-> 展开使用 `matchedGeometryEffect` 或统一状态机动画
- 重点处理动画中断（快速连点、状态连发）
- 动画时长建议：
  - 展开：0.30s
  - 收起：0.22s

### 5.4 交互
- 点击展开/收起
- 悬停预览（可选）
- Esc 或失焦收起
- 右键菜单：开机启动、主题、显示屏选择

## 6. 最小可行版本（MVP）
### 6.1 功能清单
- 顶部胶囊窗口固定显示
- 点击后展开一张静态卡片
- 平滑展开/收起动画
- 基础设置菜单（开关动画、开机启动占位）

### 6.2 验收标准
- 主流分辨率下位置正确（至少 2 种）
- 交互延迟 < 100ms（主观可接受）
- 连续 50 次展开/收起无明显错位

## 7. 分期计划
## Phase 1（1-2 天）
- 搭建窗口管理与定位
- 完成收起态 UI

## Phase 2（2-3 天）
- 完成展开态与动画状态机
- 加入基础交互（点击、失焦收起）

## Phase 3（2-4 天）
- 接入第一个动态内容源（建议：计时器或本地音乐状态）
- 完成设置页基础项

## Phase 4（1-2 天）
- 稳定性优化、边界场景（多屏、全屏）
- 补测试与打包验证

## 8. 测试策略
- 单元测试（Testing）
  - 状态机转移测试（收起/展开/中断）
  - 布局计算测试（不同分辨率）
- UI 测试（XCUIAutomation）
  - 点击展开/收起流程
  - 快速连续交互稳定性

## 9. 风险与对策
- 风险：系统窗口层级限制导致遮挡
  - 对策：分层级策略 + 可配置行为
- 风险：无统一刘海 API，机型差异导致偏移
  - 对策：引入“微调偏移”设置项
- 风险：动画中断造成状态错乱
  - 对策：单一状态机 + 可取消动画任务

## 10. 建议的下一步（针对当前项目）
1. 在 `ContentView.swift` 中先实现 `IslandView` 的收起/展开原型。
2. 新建 `IslandStateStore.swift` 管理状态。
3. 新建 `IslandWindowManager.swift` 接管窗口与定位。
4. 先接入“本地假数据 Provider”，跑通完整交互链路。

---
如果你愿意，我下一步可以直接按这个文档帮你把 `Phase 1` 的代码骨架搭好。